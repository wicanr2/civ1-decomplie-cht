# team-a — disassembly side

Inputs: Ghidra project on the user-supplied `CIV.EXE` (kept locally in `team-a/binary/`, gitignored).

Outputs: signed-off specifications in `specs/`.

Forbidden in this directory: any compilable source code (`.c`, `.cpp`, `.h`, `.cs`, `.go`, …). Specs are prose + pseudo-code + data tables only.

See `../docs/CLEAN_ROOM.md` for the full protocol.
