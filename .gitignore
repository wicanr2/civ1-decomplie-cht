# Original game binary — must never be committed
team-a/binary/
*.exe
*.EXE
!tools/**/*.exe

# Ghidra project files (kept locally in WSL ~/ghidra-projects/civ1/)
*.gpr
*.rep/
*.lock

# Extracted assets — research-only PNG sprites + thumbs are tracked
# (見 NOTICE.md 對著作權的說明)。原始 .RSC 仍由使用者自備。
# assets-extracted/ 內整個目錄入 repo 作為 CvPc decoder 正確性的證據

# Build output
team-b/build/
build/
*.o
*.obj

# Editor / OS noise
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Python tool caches
__pycache__/
*.pyc
.venv/
venv/

# User-provided raw research material (zip / 大型參考)
# 萃取結果已搬進 team-a/external/ + docs/screenshots/reference/, 原檔留本機.
user-provided-data/

# team-a/external/ 內的 .exe / .rsc binary 不 push (子 .gitignore 已細控)
# 來源: Honza Havlicek 2008 CivWin File Format demonstrator
team-a/external/civdata4_extracted/
team-a/external/Civilization/release/

# team-a/external/OpenCiv1/ — codeberg.org/rhorvat/OpenCiv1 (MIT) Team A 研究材料
# clean-room: Team B 不讀; specs 萃取後 IP chain 透過 spec 文件
team-a/external/OpenCiv1/

# R12 M8 portable build artifacts
# build_portable.sh 產出: dist/civ1-cht-linux-x64/ + tar.gz
# 真實 portable 透過 GitHub Releases 發布, 不入 git history
dist/civ1-cht-linux-x64/
dist/*.tar.gz
dist/*.zip
dist/*.exe

# R20 dev tool 產出: KING sprite 視覺辨識用 dump (含原版 1993 MicroProse 美術)
# 純本機研究用; 識別後 SLOT_TO_KING_IDX 已 hardcode 進 diplomat.c, dump 可刪
team-b/tests/_king_dump/
