# team-b — implementation side

Inputs: `team-a/specs/*.md` only.

Outputs: C99 + SDL2 source in `src/`, tests in `tests/`, translation catalogs in `assets/zh_TW/`.

Forbidden in this team's working sessions:
- Opening Ghidra or any other disassembler.
- Loading `CIV.EXE` into any tool.
- Reading any file under `team-a/dumps/` or `team-a/notes/` (specs only).
- Reading OpenCiv1, Freeciv, CivOne, or any other Civilization reimplementation.

See `../docs/CLEAN_ROOM.md` for the full protocol.
